[pytest]
addopts = -q --maxfail=1 --disable-warnings --timeout=60
markers =
    unit: fast unit tests
    smoke: quick end-to-end sanity
    rl: tests that step environments
    cli: invokes Python modules as CLIs
    slow: longer integration runs