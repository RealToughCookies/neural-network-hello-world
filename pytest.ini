[pytest]
addopts = -q --maxfail=1 --disable-warnings --timeout=60
markers =
    unit: fast unit tests
    smoke: quick end-to-end smoke
    rl: reinforcement-learning tests
    cli: command-line interface tests
    slow: longer integration runs